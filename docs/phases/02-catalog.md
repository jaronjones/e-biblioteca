# Phase 2 — Catalog & libraries

## Scope
- Library CRUD with paths under books root
- Filesystem scanner for supported formats
- Book grid/list with pagination
- Detail page and download

## Primary packages
- `internal/service/scanner`
- `internal/store` (books/libraries)
- Handlers: `/libraries`, `/books`

## Status
Done on `main` (Phase 1 Core+ plus Phase 2 format/scan test coverage).
